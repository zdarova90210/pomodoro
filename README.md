# Pomodoro

### Конфиг для File Watcher SCSS:
```
Arguments:
--no-source-map $ProjectFileDir$/styles/main.scss:$ProjectFileDir$/styles/main.css

Output patch to refresh:
$ProjectFileDir$/styles/main.css

Working directory:
$FileDir$

Scope: file:styles/*.scss
```
